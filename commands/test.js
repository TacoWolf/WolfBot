var helpers = require('./../helpers');

module.exports = {
    test: function(bot, user, userID, channelID, message) {
        //
    },
    voice: function(bot, user, userID, channelID, message) {
        //
    }
}
