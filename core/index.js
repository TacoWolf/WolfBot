

const core = require(`${__dirname}/core.js`);
const scripts = require(`${__dirname}/scripts.js`);
const graphics = require(`${__dirname}/graphics.js`);
const database = require(`${__dirname}/database.js`);
module.exports = {
  core,
  scripts,
  database,
  graphics,
};
